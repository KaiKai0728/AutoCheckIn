# 🚀 ERP 自動打卡小助手 (ERP Auto Punch-In Tool)

這是一個專為 ERP 系統設計的自動化打卡工具，支援上班簽到、下班簽退，並具備隨機延遲功能，讓打卡更自然。

## ✨ 功能特色 (Features)
* **自動化操作**：自動開啟瀏覽器、登入並尋找打卡按鈕。
* **智慧排班**：自動判斷台灣國定假日與週末（透過 API）。
* **隨機延遲**：打卡時間會隨機延遲 30~180 秒，避免準點打卡太過規律。
* **防外洩設計**：帳號密碼儲存在本地 `config.txt`，不會上傳至網路。

---

## 🛠️ 如何開始使用 (How to Use)

### 1. 下載程式
前往右側的 [Releases](https://github.com/KaiKai0728/AutoCheckIn/releases) 頁面，下載最新的 `AutoCheckInV2.exe`。

### 2. 初始化設定
* 雙擊執行 `AutoCheckInV2.exe`。
* 程式會自動在同資料夾產生一個 `config.txt`。
* 用記事本打開 `config.txt`，填入你的 ERP **帳號**與**密碼**。
* `測試模式=否` (若要正式打卡請改為否)。
* `隱藏視窗=是` (建議設為是，執行時不會彈出視窗打擾工作)。

### 3. 設定自動觸發 (重要！)
建議搭配 Windows **「工作排程器」** 設定：
* **觸發程序**：設定為「工作站鎖定時」(當你按下 Win+L 鎖屏離開位子時觸發)。
* **操作**：啟動程式，選擇 `AutoCheckInV2.exe`。
* **條件**：確保勾選「只有在網路連線時才啟動」。

---

## 📁 檔案說明
* `AutoCheckInV2.py`: 程式原始碼。
* `config.txt`: 個人設定檔（執行後產生）。
* `punch_history.txt`: 打卡紀錄 Log。

## ⚠️ 注意事項
1. 本工具僅供學術交流與提升工作效率使用，請確保符合公司相關規定。
2. 執行時請確保電腦已連網。
3. 若 ERP 介面更新，請聯繫開發者更新程式。

---
**Developed by KaiKai0728**